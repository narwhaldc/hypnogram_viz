{
  "showTitleAndDescription": true,
  "includeInToolbar": true,
  "includeInVizSwitcher": true,
  "showDrilldown": false,
  "canSetTokens": [],
  "hasEventHandlers": false,
  "config": {
    "name": "Hypnogram",
    "description": "Generic stepped lane-timeline (category-over-time). Define lanes + colors via the `lanes` option — e.g. sleep stages or HR training zones.",
    "category": "Custom",
    "icon": null,
    "dataContract": {
      "requiredDataSources": ["primary"],
      "optionalDataSources": []
    },
    "size": {
      "initialWidth": 900,
      "initialHeight": 260
    },
    "optionsSchema": {
      "backgroundFill": { "type": "string" },
      "contentOpacity": { "type": "number", "default": 0.85 },
      "slotMinutes": { "type": "number", "default": 5 },
      "lanes": { "type": "string" }
    },
    "editorConfig": []
  }
}
